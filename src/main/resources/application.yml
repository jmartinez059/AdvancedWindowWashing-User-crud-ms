spring:
  application:
    name: user-crud-ms
  datasource:
    url: jdbc:h2:mem:testdb;Mode=PostgreSQL
    platform: h2
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_HOME}
server:
  port: 8040